# Video Quality Standard

这份标准用来判断视频工厂产物是否达到“像真人制作”的最低质量线。它优先服务教程、软件演示、工具配置、比赛分析这类信息型视频。

## Core Principle

高质量不是包装更多，而是让观众相信画面是真的、流程是真的、剪辑判断是真的。

每个视频都必须先回答三个问题：

- 观众看到的是连续的视频素材，还是图片配音伪装成视频？
- 画面信息是否被新增包装遮挡？
- 每一处剪辑、推近、保留和删除是否有明确理由？

## Pass Criteria

一个可交付视频至少满足这些要求：

- 使用真实视频片段作为主体画面，不能用静态图片加配音冒充视频。
- 保留主题链路，不能为了变短破坏教程、赛事分析或叙事的完整性。
- 长版教程必须有章节曲线，不能连续堆三段以上同类界面、配置页或验证页来凑时长。
- 长版教程可以结果前置做钩子，但钩子之后正文必须沿源视频时间线推进，不能随机倒退跳剪。
- 创作增强版必须覆盖开场、上下文重置、操作链、关键决策和结果验证等导演动作。
- 创作增强版必须输出 `semantic_timeline.json`，并把推荐片段映射到可读章节；不能只给片段贴泛化角色。
- 创作增强版必须输出 `transcript_analysis.json`；有 sidecar 字幕/文稿时优先使用，没有时必须清楚标记 OCR 文本回退，不能假装做了 ASR。
- 创作增强版的冷开场必须短而准，长视频里通常不超过 `4秒`；不能把样本点前的标题卡、空白页或低信息前摇带进片头。
- 创作增强版必须限制模板感源画面占比；暗色低细节字幕页、扁平标题页、连续片尾验证页只能作为必要信息帧少量出现，不能成为主要素材。
- 长参考片创作版必须按实际入选段数满足最低长版覆盖线；避开低质/模板感画面后不能退化成短摘要。
- `cover.png` 必须来自成片真实画面，并跟随导演选中的首个样本点；不能机械取第 8 秒导致封面变成无关标题卡。
- `contact_sheet.jpg` 第一格必须是成片首帧，后续格子再均匀抽样；质检图不能和观众真实第一眼不一致。
- 不新增无意义顶部栏、进度条、章节卡、底部大面板、科技感背景或装饰头像框。
- 所有字幕、贴纸、推近、转场都必须服务信息理解，而不是证明“我做了设计”。
- 不遮挡原视频中的关键界面、人物、按钮、数据、比分、API key 区域或操作路径。
- 输出必须包含 `release.mp4`、`contact_sheet.jpg`、`cover.png` 和 `render_report.json`。
- 剪辑版必须额外包含 `edit_decision_list.md`，写明每段保留、删除和推近的理由。
- 交付前必须用 `ffprobe` 验证时长、分辨率、帧率、音频参数。
- 交付前必须跑测试，不能只凭肉眼说“应该好了”。

## AI-Looking Anti-Patterns

以下做法默认判定为高风险，除非用户明确要求：

- 给所有视频套同一套顶部标题栏、进度条和章节编号。
- 在画面底部放大块说明面板，覆盖原视频信息。
- 用装饰网格、发光线条、数据卡、虚假仪表盘包装本来真实的录屏。
- 把 9 分钟教程压成 60-90 秒摘要，却声称是同类型高质量视频。
- 为了避开低质画面而删到低于长版覆盖线，再把短摘要包装成“创作增强”。
- 反复使用标题页、字幕空页、片尾验证页来凑段数或凑收束。
- 用 AI 生成图或静态海报拼接，冒充真实比赛、真实教程或真实操作过程。
- 为了“高级感”改掉原片主题、顺序和信息密度。
- 没看参考片内容，只按通用短视频模板生成。

## Acceptable Enhancements

这些增强通常是安全的：

- 轻微锐化、对比度、亮度、饱和度调整。
- 音频响度标准化、低频噪声过滤、高频杂音削弱。
- 删除等待、重复、无信息停顿。
- 对关键界面做自然数字推近，通常控制在 `1.05x` 到 `1.15x`。
- 保留原片字幕、人物小窗、鼠标路径等本来存在的内容。
- 生成封面和抽帧图用于质检。

## Output Targets

教程和软件演示类视频：

- 原片增强版：保留原时长或接近原时长。
- 真人剪辑版：通常保留原片 `65%` 到 `85%` 时长。
- 短视频摘要版：只有用户明确要求时才做，且必须说明会牺牲完整教程感。

技术参数：

- 横屏默认 `1920x1080`、`30fps`。
- 竖屏默认 `1080x1920`、`30fps`。
- H.264 输出建议使用 CRF 18-20。
- 音频建议 AAC、44100Hz、双声道、128k-192k。

## Evidence Required

每次交付必须提供证据：

- `ffprobe` 参数摘要。
- 测试结果，例如 `83 passed in 11.74s`。
- `contact_sheet.jpg` 视觉抽检。
- `cover.png` 与 `contact_sheet.jpg` 第一格必须一起看，确认真实片头没有被封面或抽样逻辑掩盖。
- 如果是剪辑版，提供 `edit_decision_list.md`。
- 如果是创作增强版，提供 `creative_plan.json`、`semantic_timeline.json`、`transcript_analysis.json`、`candidate_edl.md` 和 `quality_report.json`。

## Lessons From The CC Switch Case

失败版的问题：

- 把“高质量”误解成“加包装”。
- 顶部栏、进度条、章节卡和底部说明面板让视频一眼像模板。
- 90 秒摘要破坏了 9 分钟教程的完整感。

成功版的改法：

- 原片增强版保留 `9分23秒`，只做画质和音频处理。
- 真人剪辑版从 `9分23秒` 剪到 `7分17秒`，保留 10 个真实片段。
- 关键画面只做轻微数字推近，不新增说明框。
- 输出包含 EDL、质检图、报告和测试验证。
- 创作版必须把首段样本点、冷开场长度、封面取点和质检图首格一起校验；只看 `quality_report.json` passed 不等于真的能交付。
- 创作版还必须检查模板感画面预算和长版时长底线；清掉“AI味”不能以牺牲教程完整度为代价。
