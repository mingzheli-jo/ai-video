# Human Editing Playbook

这份手册定义“真人剪辑版”的工作方法。目标不是把视频做得更花，而是让观众感觉这是一个有判断力的剪辑师剪出来的。

## Editing Philosophy

真人剪辑的核心是判断：

- 哪些内容必须保留，因为它承载主题。
- 哪些内容可以删除，因为它只是等待、重复或低信息密度。
- 哪些画面需要推近，因为观众需要看清操作。
- 哪些地方应该保持原样，因为过度处理会破坏真实感。

不要用包装弥补剪辑判断。剪辑本身要成立。

## Recommended Structure

教程和软件演示类视频建议使用这个结构：

1. 开场问题或目标。
2. 工具或背景介绍。
3. 安装、下载或入口说明。
4. 关键配置流程。
5. 账号、API、权限、模型等风险点。
6. 本地验证或真实运行。
7. 结尾收束，说明流程已经跑通。

如果原片本来就是教程，不要强行改成营销短片。先保留教程完整感，再优化节奏。

## EDL Is Mandatory

剪辑版必须输出 `edit_decision_list.md`。每一行至少包含：

- 源视频入点。
- 保留时长。
- 镜头处理，例如 `1.00x`、`1.10x`。
- 剪辑意图。

示例：

```markdown
| # | Source In | Duration | Lens | Edit Intent |
|---|---:|---:|---:|---|
| 7 | 06:05 | 00:50 | 1.12x | 推近 API key 管理页面，突出关键配置动作。 |
```

没有 EDL 的剪辑不可交付，因为无法解释为什么这么剪。

## Segment Selection Rules

优先保留：

- 开头建立主题的内容。
- 第一次出现核心工具、核心人物、核心比赛或核心观点的画面。
- 操作路径中不可跳过的页面。
- API key、模型选择、配置开关、比分、数据图、关键按钮等观众必须看清的画面。
- 验证成功、最终结果或结尾收束。

优先删除：

- 长时间等待加载。
- 重复解释同一个概念。
- 鼠标停留但没有新信息的片段。
- 为了铺垫而铺垫的内容。
- 与主题链路无关的闲聊。

谨慎删除：

- 失败和排错过程。它们有时会增强真实感。
- 配置前后对比。它们有时是理解流程的关键。
- 原片自带的人像、字幕和鼠标轨迹。它们通常是可信度的一部分。

## Digital Punch-In Rules

数字推近只能用于帮助看清信息。

推荐范围：

- 普通界面：`1.05x` 到 `1.08x`。
- 关键设置、表单、API、模型列表：`1.10x` 到 `1.15x`。
- 超过 `1.20x` 需要谨慎，容易显得粗糙和压迫。

推近时必须注意：

- 不能裁掉鼠标、关键按钮、标题、表单字段。
- 不能让原片字幕被切掉。
- 不能频繁推近，避免像自动模板。
- 同一段里不要来回缩放，除非是明确的强调动作。

## Audio Rules

教程类视频优先保留原声，除非用户明确要重配音。

允许处理：

- 响度标准化。
- 降低低频噪声。
- 削弱高频刺耳噪声。
- 处理切点附近的音频时间轴连续性。

默认禁止：

- 用 AI 配音覆盖原作者声音。
- 用背景音乐掩盖信息型讲解。
- 用音效证明剪辑存在。

## Visual Rules

默认不要新增：

- 顶部标题栏。
- 进度条。
- 大号章节卡。
- 底部说明面板。
- 科技网格、发光背景和虚假数据面板。
- 新增头像框或虚拟主持人。

可以保留：

- 原片自带字幕。
- 原片自带人像小窗。
- 原片自带操作提示。
- 原片自带转场。

可以轻微调整：

- 亮度。
- 对比度。
- 饱和度。
- 锐化。
- 编码质量。

## CC Switch Human Edit Template

本项目中的成功样片：

- 源视频：`/Users/king/Downloads/下载 (1).mp4`
- 输出：`video_factory/output/cc-switch-deepseek-human-edit/release.mp4`
- 原片时长：`9分23秒`
- 剪辑后时长：`7分17秒`
- 分段数：`10`
- 推近段数：`7`

剪辑骨架：

1. `00:00` 保留开场问题。
2. `00:56` 跳到 Codex 背景。
3. `01:58` 进入 CC Switch 统一入口。
4. `03:10` 保留版本下载和更新。
5. `04:20` 进入服务商配置。
6. `05:15` 保留 DeepSeek 官网/API 入口。
7. `06:05` 推近 API key。
8. `07:05` 推近本地配置。
9. `08:05` 保留模型选择。
10. `08:52` 保留验证和结尾。

这个模板可以复用到其他教程类视频，但每次都要重新看原片，不能机械套时间点。

## Review Checklist

交付前逐项检查：

- 成片是否仍然讲得通？
- 删掉的内容是否真的低价值？
- 推近是否帮助理解，而不是制造存在感？
- 有没有新增 AI 模板味包装？
- `contact_sheet.jpg` 是否能看出完整流程？
- `edit_decision_list.md` 是否能解释每一刀？
- 音画是否连续，切点是否突兀？
- 测试和 `ffprobe` 是否已经跑过？
