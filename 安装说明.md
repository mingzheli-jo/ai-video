# King-AI-video 新机器安装说明（Windows）

> 目标：在一台全新的 Windows 电脑上，从拉代码到出第一支视频。
> 全程约 10~20 分钟（首次运行会额外下载 whisper 模型和无头 Chrome，见文末）。

## 一、先装三个系统级软件

| 软件 | 用途 | 下载 | 安装要点 |
|------|------|------|---------|
| Python 3.13+ | 整条流水线 | https://www.python.org/downloads/ | 安装时**务必勾选 "Add Python to PATH"** |
| Node.js LTS | Remotion 特效/封面渲染 | https://nodejs.org/ | 默认下一步即可（自带 npm/npx） |
| ffmpeg | 拼装/字幕/封面抽帧 | https://www.gyan.dev/ffmpeg/builds/（选 release full） | 解压后把 `bin` 目录加进系统 PATH |

装完后开一个**新的** PowerShell 验证（旧窗口读不到新 PATH）：

```powershell
python --version   # 出版本号即可
node --version
ffmpeg -version
```

## 二、拉代码 + 一键安装

```powershell
git clone https://github.com/mingzheli-jo/ai-video.git
cd ai-video
```

然后**双击项目根目录的 `install.bat`**。它会自动：

1. 检查 python / node / ffmpeg 是否就位（缺哪个会明说）；
2. 创建 `.venv` 虚拟环境并安装全部 Python 依赖（默认走清华镜像，国内快）；
3. 进 `remotion/` 执行 `npm install` 装特效渲染依赖；
4. 建好 `music/` 空文件夹（BGM 下拉的来源）；
5. 末尾输出一张依赖体检表。

> npm 太慢时可先执行 `npm config set registry https://registry.npmmirror.com` 换国内镜像再重跑。

## 三、手动拷贝三样（不走 git，从老电脑带过来）

| 内容 | 位置 | 说明 |
|------|------|------|
| `credentials.yaml` | 项目根目录 | 全部密钥（豆包生图 ARK、豆包 TTS、DeepSeek）。**不拷也行**：启动后在工作台「凭据与依赖」页重填一遍即可。该文件含明文密钥，切勿提交进 git（.gitignore 已挡） |
| `music/` 里的音乐文件 | 项目根目录 | BGM 下拉列表的来源，mp3/wav 直接丢进去 |
| `素材库/`（可选） | 项目根目录 | AI 生图的积累图库。不拷则新机器从零积累，不影响功能 |

## 四、启动与验证

1. 双击 **`启动创作工作台.bat`**（会自动打开浏览器 http://127.0.0.1:56090/）；
2. 进「凭据与依赖」页：四个依赖灯（ffmpeg / node / faster-whisper / edge-tts）应全绿，凭据点亮所需项；
3. 提交一支测试视频。

## 五、首次运行会慢的两个点（正常现象）

- **首次转写/对齐**：faster-whisper 自动下载模型（几百 MB），之后缓存在本机；
- **首次渲染特效/封面**：Remotion 自动下载无头 Chrome，之后不再下载。

两者都需要网络，第一支视频比平时慢几分钟属正常。

## 六、常见问题

| 现象 | 原因与解法 |
|------|-----------|
| `install.bat` 提示找不到 python/node/ffmpeg | 对应软件没装或没进 PATH；装完后**重开**一个终端/重新双击 |
| `启动创作工作台.bat` 闪退 | 先跑 `install.bat`（缺 `.venv` 会闪退）；仍闪退就在 PowerShell 里手动执行 `.venv\Scripts\python.exe -m video_factory.studio` 看报错 |
| pip 安装超时 | `install.bat` 默认已走清华镜像；公司网络拦镜像时，编辑 install.bat 删掉 `-i https://...` 参数改走官方源 |
| 生图/配音报"未配置凭据" | 「凭据与依赖」页补填对应 key（或拷贝老机器的 credentials.yaml 后重启服务） |
| 字幕字体不对 | 字幕用微软雅黑/黑体/楷体等 Windows 自带字体，正常无需安装；精简版系统缺字体时装回即可 |
